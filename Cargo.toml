[workspace]

members = [
    "geo",
    "utils",
    "softy",
    "softy-hdk",
    "implicits",
    "cimplicits",
    "implicits-hdk",
    "benchmark",
]

exclude = [
    "utils/soap-derive",
]

[profile.release]
debug = false # Change this to true to enable debug symbols in release mode
rpath = false
