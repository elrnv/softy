[package]
name = "cimplicits"
version = "0.2.4"
authors = ["Egor Larionov <egor.larionov@gmail.com>"]
edition = "2018"

[lib]
name = "cimplicits"
crate-type = ["cdylib", "rlib"]

[dependencies]
implicits = { path = "../implicits" }
reinterpret = "0.2"
gut = { version = "0.2", features = ["io"] }

[build-dependencies]
cbindgen = "0.6"
