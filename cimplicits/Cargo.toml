[package]
name = "cimplicits"
version = "0.2.1"
authors = ["Egor Larionov <egor.larionov@gmail.com>"]
edition = "2018"

[lib]
name = "cimplicits"
crate-type = ["cdylib", "rlib"]

[dependencies]
implicits = { path = "../implicits" }
reinterpret = "0.1"
geometry = { git = "ssh://git@gitlab.com/elrnv/geometry.git", features = ["io"] }

[build-dependencies]
cbindgen = "0.6"
